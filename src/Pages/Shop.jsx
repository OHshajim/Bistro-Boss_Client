import { Helmet } from "react-helmet-async";

const Shop = () => {
    return (
        <div>
            <Helmet>
                <title>Bistro Boss || Our Shop</title>
            </Helmet>
        </div>
    );
};

export default Shop;